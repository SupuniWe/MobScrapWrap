<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/mainpagebackground"
    tools:context="com.db.cdap.scrapwrap.MainActivity">

    <ImageView
        android:id="@+id/imageViewHeading"
        android:layout_width="wrap_content"
        android:layout_height="400dp"
        android:layout_above="@+id/btnOldUser"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="269dp"
        app:srcCompat="@drawable/font" />

    <Button
        android:id="@+id/btnOldUser"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="70dp"
        android:background="@drawable/input_background"
        android:text="Login" />

    <Button
        android:id="@+id/btnNewUser"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_marginTop="500dp"
        android:background="@drawable/input_background"
        android:text="Register" />


</RelativeLayout>
